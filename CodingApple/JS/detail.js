// 버튼O 누르면
// - 모든 버튼에 붙은 orange 클래스명 제거
// - 버튼O에 orange 클래스명 추가
// - 모든 div에 붙은 show 클래스명 제거
// - divO에 show 클래스명 추가
document.querySelector(".tab-button").addEventListener("click", function () {
  // 숙제. 버튼 3개 기능 전부 숙제로 해오기
});

// jQuery //

//     querySelectorAll('.tab-button')[0]과 비슷함
// $(".tab-button")
//   .eq(0)
//   .on("click", function () {});
